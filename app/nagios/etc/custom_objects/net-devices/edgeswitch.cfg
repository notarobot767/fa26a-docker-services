define service {
    use                     og-generic-service
    host_name               edgeswitch
    service_description     http
    check_command           check_http!-H switch.ogrydziak.net -4 -d "Location: https://switch.ogrydziak.net"
}

define service {
    use                     og-generic-service
    host_name               edgeswitch
    service_description     https
    check_command           check_https!-H switch.ogrydziak.net -4 -s "<title>Ubiquiti EdgeSwitch</title>"
}
